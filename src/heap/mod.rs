/// Fractal heap for variable-length objects (v2 groups, dense attributes).
pub mod fractal;
/// Global heap for shared objects (vlen strings, references).
pub mod global;
/// Local heap for link names (v1 groups).
pub mod local;
